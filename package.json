{
  "private": true,
  "name": "devvit-backend",
  "version": "0.0.0",
  "license": "BSD-3-Clause",
  "type": "module",
  "workspaces": [
    "web-view-app"
  ],
  "scripts": {
    "build:webroot": "npm run build:webroot --workspace=web-view-app",
    "playtest": "npm run build:webroot --workspace=web-view-app && devvit playtest r/axolotl_playground",
    "dev": "npm run dev --workspace=web-view-app",
    "upload": "npm run build:webroot --workspace=web-view-app && devvit upload"
  },
  "dependencies": {
    "@devvit/payments": "^0.11.7",
    "@devvit/public-api": "0.11.7"
  },
  "devDependencies": {
    "typescript": "5.3.2"
  }
}
