{
  "private": true,
  "name": "devvit-backend",
  "version": "0.0.0",
  "license": "BSD-3-Clause",
  "type": "module",
  "workspaces": [
    "web-view-app"
  ],
  "scripts": {
    "build": "npm run build:webroot --workspace=web-view-app",
    "dev": "npm run build && npm run dev --workspace=web-view-app",
    "playtest": "node commands/playtest.cjs",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@devvit/payments": "^0.11.11",
    "@devvit/public-api": "0.11.11"
  },
  "devDependencies": {
    "typescript": "5.3.2",
    "vite": "^6.2.4",
    "vitest": "^3.1.1"
  }
}
